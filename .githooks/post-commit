#!/bin/sh
# Skip recursion — npm version minor creates a commit with message like "0.2.0"
last_msg=$(git log -1 --pretty=%s)
case "$last_msg" in
  [0-9]*.[0-9]*.[0-9]*) exit 0 ;;
esac
npm version minor
